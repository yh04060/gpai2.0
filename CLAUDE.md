# GPAI 2.0 — 프로토타입

전세계 대학생을 위한 학업 AI 도구 **GPAI**의 인터랙티브 프로토타입입니다.
실제 AI 호출이나 데이터 저장은 없고, 모든 응답·파일·수치는 데모입니다.

> 포지셔닝의 핵심은 "공부를 도와준다"는 추상적 가치가 아니라,
> **대학생이 반복하는 학업 작업을 AI로 자동화해 "AI로 학점 상승"이라는 결과를 파는 것**입니다.
> 모든 화면은 그 약속을 구체적인 워크플로우로 보여주기 위해 존재합니다.

Q는 컴퓨터공학 전공자이므로 기술적 설명은 추상적 비유 대신 실제 메커니즘(권한 모델,
동시성, 렌더링 파이프라인 등)으로 해도 됩니다. 다만 특정 서비스나 도구를 언급할 때는
그게 무엇인지부터 설명하고 내부 동작으로 들어가세요.

---

## 프로젝트 구조 — 정적 멀티파일, 빌드 없음

**저장소가 곧 배포물입니다.** 빌드 도구·번들러·npm 의존성이 전혀 없고,
`index.html`이 `css/`와 `js/`의 파일들을 표준 `<link>`/`<script src>`로 로드합니다.
GitHub Pages 같은 정적 호스팅에 저장소를 그대로 올리면 동작합니다.

```
gpai2.0/
├── CLAUDE.md       이 문서
├── .nojekyll       GitHub Pages가 Jekyll 처리를 건너뛰게 하는 빈 파일
├── index.html      마크업 전체 (~1,600줄) — head · 사이드바 · 화면 9종 · 편집기 · 온보딩 · 오버레이
├── assets/         이미지 파일 — avatars/a01~a20.png (프로젝트 AI 프로필, 512×512)
├── css/            스타일 18개 — 화면/컴포넌트 섹션별 (06번은 결번)
└── js/             스크립트 20개 — 데이터 · 라우팅 · 화면별 로직
```

### 실행

```bash
python3 -m http.server 8000   # → http://localhost:8000  (배포 환경과 동일, 권장)
```

`index.html` 더블클릭도 동작합니다 — 클래식 스크립트와 상대경로 CSS는 `file://`에서도
로드됩니다 (ES 모듈로 전환하면 이게 깨지니, 전환 시엔 로컬 서버가 필수가 됩니다).

- 기준 환경: **1920×1080 PC 크롬**
- 외부 의존은 Google Fonts 링크 하나뿐 (오프라인에서도 폴백 폰트로 정상 렌더)

### 파일 로드 규칙 — 가장 중요한 구조 지식

- **js 20개는 하나의 전역 스코프를 공유하는 클래식 스크립트**입니다 (모듈 아님).
  `index.html` 하단의 `<script src>` 나열 순서가 곧 실행 순서이고,
  **정의(`const`·`function`)가 사용보다 먼저 오도록 순서를 지켜야 합니다**
- 숫자 접두사(`00-` ~ `16-`)는 이 로드 순서를 파일명에 드러낸 것입니다.
  새 파일은 의존하는 파일보다 큰 번호를 붙이고 `index.html`의 해당 위치에 끼워 넣으세요
- CSS도 같은 방식입니다. `00-base.css`의 토큰(`:root` 변수)을 나머지 전부가 참조합니다

### 배포 — GitHub Pages (이미 활성화됨)

GitHub Pages는 GitHub가 저장소의 정적 파일을 그대로 웹사이트로 서빙해 주는 호스팅입니다.
이 저장소는 빌드가 없으므로 파일이 그대로 올라갑니다.

| 항목 | 값 |
|---|---|
| 사이트 | **https://yh04060.github.io/gpai2.0/** |
| 소스 | `main` 브랜치 루트(`/`) — Settings → Pages |
| 트리거 | **`main`의 커밋이 바뀌면 자동 재배포** (다른 브랜치 푸시로는 배포되지 않음) |
| 소요 | 30초 내외. `Actions` 탭의 `pages build and deployment`에서 이력 확인 |

개발도 `main`에서 하므로 **푸시하는 순간이 곧 배포**입니다. 검증을 마치고 푸시하세요
(→ 「작업 흐름 · 안전장치」). 임시 작업 브랜치를 판 경우에만 `main` 머지 시점이 배포 시점입니다.

---

## 제품 구조 — 3단 에이전트 위계

제품 전체가 이 구조 하나로 설명되고, 사이드바 순서도 이 위계 그대로입니다.
마스터 AI는 유저 자신을 의인화한 것이라 계정과 같은 위계이므로, **사이드바 최상단의
계정 카드(`.sb-me`)가 나 = 마스터 AI = AI 드라이브(1단)를 한 덩어리로 묶어 보여줍니다**
(저장용량 게이지는 드라이브 화면 상단 배너에 있고, 하단 프로필 블록은 없습니다).

| 단계 | 에이전트 | 권한과 맥락 |
|---|---|---|
| **1** | **AI 드라이브 · 마스터 AI** | 내가 GPAI를 쓰는 동안 쌓인 **모든 데이터**에 접근. 무엇이든 시키고 모든 보고를 받는 개인 커맨드 센터 |
| **2** | **프로젝트 · 프로젝트 AI** | 드라이브의 **폴더 하나**를 지정해 생성. 그 폴더 하위 전체에 접근하고 **폴더 밖은 접근 불가**. 맥락도 그 채널의 대화만 사용 |
| **3** | **툴 에이전트** | 문제 풀이 · 문제 생성 · 시각화 · 캔버스 · 문서 작성 · PPT 제작 · 채팅. 프로젝트 AI가 부리는 도구이자, 사이드바에서 직접 쓸 수도 있는 기능 단위 |

**학교로 비유하면** — 마스터 AI는 *나 자신을 의인화한 것*(전 학기에 걸쳐 쌓이는 자료가
전부 여기 들어감), 프로젝트는 *한 과목*(일반물리학 폴더에 권한을 줘 "일반물리학 AI"를
만드는 것), 툴은 *그 과목을 들으며 실제로 하게 되는 일*입니다.
과목 조교가 다른 과목 자료를 볼 이유도 권한도 없다는 점이 2단계 샌드박스의 근거입니다.

프로젝트 AI와 나눈 대화는 계속 요약되어 그 에이전트의 메모리(`memory.md`)에 쌓입니다.
쓸수록 그 과목을 더 잘 아는 조교가 되는 구조이고, 대화 UX를 슬랙 채널로 잡은 것도
**게시글당 1이슈**가 유지되어 맥락이 뒤섞이지 않고 나중에 되짚기 쉽기 때문입니다.

### 화면 목록 (16)

주소 끝에 해시를 붙이면 해당 화면으로 이동합니다.
**해시는 초기화 시점에만 읽히므로, 이동 후에는 반드시 새로고침해야 합니다.**
**해시가 없으면 온보딩이 자동으로 뜹니다** — 첫 방문 경험이 기본값입니다.

| 구분 | 해시 | 화면 | 마크업 위치 (`index.html`) |
|---|---|---|---|
| 핵심 | `#master` | 나의 마스터 AI | `#view-master` 섹션 |
| | `#drive` / `#drive-list` | AI 드라이브 (그리드 / 리스트) | `#view-drive` 섹션 |
| | `#project-p1` | 프로젝트 채널 | `#view-project` (JS가 동적 렌더) |
| 툴 | `#solver` | 문제 풀이 | `#view-solver` 섹션 |
| | `#generator` | 문제 생성 (BETA) | `#view-generator` 섹션 |
| | `#figure` | 시각화 (도식화) | `#view-figure` 섹션 |
| | `#canvas` | 캔버스 | `#view-canvas` 섹션 |
| | `#chat` | 채팅 | `#view-chat` 섹션 |
| | `#ppt` | PPT 제작 (BETA) — 자료 → 개요 → 슬라이드 | `#view-ppt` 섹션 |
| | `#report-writer` | 문서 작성 — 전용 화면 없음 | `#view-generic` 플레이스홀더가 이름만 바꿔 뜸 |
| 편집기 | `#edit-doc` | DOCX · 실험 보고서 | `#editor` 오버레이 (5종 공통) |
| | `#edit-xlsx` | XLSX · 성적 관리 | `#editor` |
| | `#edit-ppt` | PPTX · 물리학 발표 | `#editor` |
| | `#edit-pdf` | PDF · 중간고사 기출 | `#editor` |
| | `#edit-yt` | YouTube 뷰어 | `#editor` |
| 기타 | `#onboarding` (해시 없음도 동일) | 온보딩 — 가입 → LMS 연결 → 자동 임포트 | `#obRoot` 오버레이 · `#onboarding-0`~`-5`로 스텝 직행 |
| | `#guide` | 「Q의 의견」 드로어 자동 열림 | `#noteDrawer` |

드라이브의 **폴더 내부 뷰는 별도 화면이 아니라 `state.drivePath` 상태**입니다
(해시 없음 · `#drive`에서 폴더 클릭). 1단계 폴더의 내용물은 연결 프로젝트의
`files`가 정본이라 프로젝트 파일 탭과 항상 일치하고, 미연결 폴더만 `FOLDER_FILES`를
씁니다 — 폴더 관련 수정 시 두 출처를 함께 확인하세요.

라우팅 채널은 `VIEWS` 배열 하나가 아니라 둘입니다. `AGENTS`(`js/03-data.js`)에 등록된
키는 `VIEWS`에 없어도 해시로 라우팅되며, 이때 `sync()`(`js/05-router.js`)가
`#view-generic` 플레이스홀더를 켜고 제목만 갈아끼웁니다 — 문서 작성이 이 경로입니다.
전용 화면을 만들 때는 `AGENTS`에서 빼고 `VIEWS`로 옮기는 작업이 됩니다.

### 설계 원칙

- **자료 축적 → AI 활용 → 결과물 축적** 루프가 돌아야 합니다. 그래서 에이전트 결과물은
  전부 드라이브(프로젝트 폴더)에 자동 저장됩니다
- **화이트+그레이의 담백한 톤.** 매일 오래 쓰는 화면이라 장식보다 밀도와 익숙함이
  중요하고, 유저가 이미 아는 드라이브 멘탈모델을 그대로 빌립니다
- **주황(`#EE7732`)은 "AI가 개입하는 순간"에만.** 로고·활성 상태·전송 버튼·AI 생성 태그
- **세리프는 브랜드의 목소리.** 워드마크와 에이전트 랜딩 타이틀에만 써서
  학문적·에디토리얼한 인상을 만듭니다
- **모델 중립이 제품 전략.** GPAI는 특정 모델 회사가 아니라 학업 워크플로우 레이어입니다
- **첨부는 "가져오기"가 아니라 "선택".** 드라이브는 이미 유저의 공간이므로
- **시작은 온보딩 — "링크나 토큰만 줘, 우리가 다 해줄게".** 학기 자료 업로드와
  프로젝트 생성을 유저 손에 맡기면 아무도 안 하므로, LMS(eTL·Canvas 등)를 연결해
  파일 임포트 → 과목별 폴더 정리 → 과목별 프로젝트 생성까지 자동으로 해줍니다.
  임포트가 끝난 상태가 프로토타입의 기본 데이터이고, 온보딩은 그 과정을 연출합니다.
  단, **LMS 연결은 옵션입니다** — 대학생이 아닌 유저를 위해 「LMS 없이 시작하기」가
  동급 경로로 제공되고, 드라이브 「신규」 메뉴의 "LMS에서 가져오기"(학교 자료)와
  "Google Drive에서 가져오기"(개인 자료)가 나중 반입 경로입니다

---

## 코드를 수정하기 전에

### 맥락의 정본은 코드 안에 있습니다

문서보다 코드가 우선입니다. **이 문서와 코드가 다르면 코드가 맞습니다.**
작업 시작 전 반드시 다음 세 가지를 확인하세요.

1. **버전 로그** (`index.html` 최상단 주석) — v1부터 현재까지 무엇이 언제 왜 바뀌었는지 전부
2. **브라우저로 열고 하단 「Q의 의견」 버튼** — 제품 정의, 포지셔닝, 3단 위계,
   설계 원칙, 아직 목업인 영역, 받고 싶은 피드백
3. **각 화면의 드로어 "지금 화면" 섹션** (`js/08-notes.js`의 `NOTE_CTX`) —
   16개 화면마다 설명 · **왜 이렇게 만들었나** · 해볼 것 · 작성자의 질문.
   **화면별 설계 의도의 정본입니다**

### 절대 원칙

1. **의존성 제로 유지.** 빌드 도구·번들러·npm 패키지·프레임워크 없이 바닐라
   HTML/CSS/JS로만 갑니다. 외부 의존은 Google Fonts 링크 하나뿐입니다.
   도입이 필요해지면 먼저 Q와 상의하세요
2. **기준 환경은 1920×1080 PC 크롬**
3. **새 화면을 추가하면 `NOTE_CTX`에 항목을 반드시 추가합니다.**
   필드는 `name / desc / why / try[] / ask`. 드로어에 설명이 없는 화면이 있으면 안 됩니다

**처음부터 다시 만들지 마세요.** 기존 구조·스타일·네이밍 규칙을 따라 요청받은 부분만
수정합니다.

---

## 코드 지도

### `index.html` — 마크업 (위에서 아래로)

버전 로그 주석 → `<head>`(메타 · 폰트 · css 18개 링크) → `.app`(사이드바 →
`view-*` 섹션 9종 → 드라이브 챗 패널 → 프로필 패널 `#pfPanel`) → `#editor` 오버레이(공통 프레임 + 본문 5종 +
GPAI Chat) → 온보딩 오버레이(`#obRoot`) → 업로드 토스트 → Q의 의견 드로어(기밀 공지 포함) →
Google Drive 모달 / 「신규」 메뉴 / 파일 ⋯ 메뉴 / 첨부 메뉴 / 드라이브 피커 /
새 프로젝트 모달 → js 20개 스크립트 태그

### `css/` — 스타일 (18 · `06-` 결번)

| 파일 | 담당 |
|---|---|
| `00-base.css` | 리셋 · 토큰(`:root` 색/폰트 변수, `color-scheme`) · `.app`/`.main`/`.view` |
| `01-sidebar.css` ~ `04-upload-toast.css` | 사이드바 · 드라이브 · 챗 패널 · 업로드 토스트 |
| `05-hierarchy.css` | **3단 위계(마스터·프로젝트·파일 트리) — 가장 큼** |
| `07-note-drawer.css` | Q의 의견 드로어 + 최상단 기밀 공지 + 하단 중앙 노트 버튼 (`06-`은 기밀 바와 함께 v7.11에서 삭제 — 번호는 재정렬하지 않고 비워둠) |
| `08-solver.css` ~ `12-chat.css` | 툴 화면들 (솔버 · 제너레이터 · visuals · 캔버스 · 챗) |
| `13-editor.css` | 편집기 5종 + GPAI Chat |
| `14-attach-picker.css` · `15-generic.css` | 첨부 메뉴/피커 · 플레이스홀더 |
| `16-onboarding.css` | 온보딩 오버레이 + 드라이브 재진입 배너 (`.ob-` 접두사) |
| `17-ppt.css` | PPT 제작 (`.pmk-` 접두사 — `.ppt-`는 편집기가 사용 중) |
| `18-profile.css` | 슬랙형 프로필 패널(`.pf-`) + 유저 사진 `<img>` 채움 규칙(`.uav`) |

### `js/` — 스크립트 (20, 로드 순서대로)

| 파일 | 담당 | 주요 심볼 |
|---|---|---|
| `00-helpers.js` | DOM 헬퍼 | `$` `$$` |
| `01-assets-drive.js` | 드라이브 아이콘·썸네일 SVG | `SVG_*` `fIcon` |
| `01-assets-avatars.js` | **프로젝트 AI 프로필 20종** — `assets/avatars/*.png` 레지스트리 + 렌더러(`<img>`) + 내 아바타. 같은 01번대(자산)라 순서 무관 | `AVATARS` `avatarSVG` `avatarFor` `avatarDefault` `userAvatar` |
| `02-canvas.js` | 캔버스 갤러리 | `CV_ITEMS` `renderCanvas` |
| `03-data.js` | **핵심 상태·라우팅 데이터** + 데모 계정 | `driveItems` `AGENTS` `VIEWS` `state` `USER` |
| `04-drive.js` | 드라이브 렌더 · 폴더 탐색 · 파일 ⋯ 메뉴 | `renderDrive` `driveResolve` `driveOpenFolder` |
| `05-router.js` | **뷰 전환** | `sync` `go` |
| `06-misc-controls.js` | 챗 패널·보기 토글·전송 버튼 | |
| `07-editor-open.js` | 편집기 열기/닫기 · ESC 체인 | `openEditor` `closeEditor` |
| `08-notes.js` | **드로어 메모(화면별 설계 의도)** | `NOTE_CTX` `renderNoteCtx` |
| `09-editor-body.js` | 편집기 본문(시트·PPT·PDF·YT) · 모델 선택 · 챗 | `MODELS` `edSend` |
| `10-attach-picker.js` | 첨부 메뉴 · 드라이브 피커 | `openAttMenu` `openPicker` |
| `11-projects.js` | **프로젝트 전체** — 채널·쓰레드(라이브 툴 호출)·파일 트리·새 프로젝트 모달 | `PROJECTS` `FOLDER_FILES` `PJ_ROUTES` `renderProject` `pjSendMsg` |
| `12-master.js` | 마스터 AI 입력 | `maSend` |
| `13-upload.js` | 업로드 라우팅(드라이브/프로젝트) | `startDriveUpload` `upFinish` |
| `14-init.js` | 해시 처리 · 초기 구동 | |
| `15-onboarding.js` | **온보딩 전체** — 스텝 전환·LMS 폼·임포트 연출·진입 판단(자체 해시 처리) + 드라이브 「신규」 메뉴 | `OB_COURSES` `openOnboard` `closeOnboard` `obGo` |
| `16-gdrive.js` | Google Drive 가져오기 모달 — 연결 연출·다중 선택 → 업로드 토스트로 전달 (`#gdrive` 해시 처리) | `GD_FILES` `openGdPicker` `closeGdPicker` |
| `17-ppt.js` | **PPT 제작** — 랜딩(내 덱)·개요 생성 연출·덱 스타일/템플릿·슬라이드 생성→드라이브 저장·진행 로그 레일 | `PMK_OUTLINE` `PMK_TPLS` `pmkStart` `pmkGenerate` |
| `18-profile.js` | **프로필 패널** — AI 프로필(온라인·담당 폴더·메모리·툴·사진/이름 변경) · 내 프로필(사진 업로드/캐릭터 선택). `data-pf` 클릭 위임, `state.profile`이 정본 | `openProfile` `closeProfile` `renderUser` |

---

## 수정 방식

- 파일이 작으므로 **일반적인 편집 도구로 직접 수정**하면 됩니다.
  어느 파일을 고칠지는 위 코드 지도에서 찾으세요
- 여러 곳에 걸친 대규모 치환은 여전히 **매칭 횟수를 세고, 기대값과 다르면 파일을
  쓰지 않는** 방식이 안전합니다:

```python
import sys
h=open(path,encoding='utf-8').read()
R=[('<원본 문자열>','<새 문자열>',1)]   # (old, new, expected_count)
n=0
for o,new,c in R:
    got=h.count(o)
    if got!=c: print('MISMATCH(%d/%d): %r'%(got,c,o[:70])); n+=1
    else: h=h.replace(o,new)
if n: sys.exit(1)                        # 하나라도 어긋나면 파일을 쓰지 않는다
open(path,'w',encoding='utf-8').write(h)
```

문자열 앵커는 **공백과 줄바꿈까지 정확히** 일치해야 합니다.
매칭에 실패하면 추측하지 말고 `grep -n` / `sed -n 'X,Yp'`로 실제 바이트를 먼저 확인하세요.

### 그래픽·이미지 작업 방식 (프로젝트 AI 프로필 캐릭터가 선례)

UI 아이콘·썸네일·문서 목업은 **인라인 SVG**로 그립니다(빌드·의존성 없이 선명하고,
색을 코드로 통제). **캐릭터·일러스트 같은 "그림"은 이미지 파일**을 씁니다 —
프로젝트 AI 프로필이 선례이고, Q가 수학대왕 아바타 PNG 20종을 제공했습니다.

**이미지 자산은 `js/01-assets-avatars.js` 패턴을 따르세요:**

- **파일은 `assets/<용도>/`에, 참조는 레지스트리로.** `AVATARS`는 `{k, n, src}` 항목의
  배열이고 `avatarSVG(k,size)`가 그리는 유일한 함수입니다(이름은 역사적 이유로 SVG지만
  `src`가 있으면 `<img>`를 냅니다). 화면 코드는 키만 다룹니다(`p.avatar='a07'`).
  파일을 갈아끼워도 화면 코드 수정은 0입니다
- **파일명은 키와 같게** (`a07` ↔ `assets/avatars/a07.png`). 원본 파일명이 길면
  레지스트리 쪽에서 짧은 키로 바꾸고, 사람이 읽을 이름은 `n`에 둡니다
- **배경까지 파일 안에.** 아바타 PNG는 배경색이 구워져 있어 컨테이너는
  `background:transparent` + `overflow:hidden` + `border-radius`만 줍니다
  (`.pj-pava.av` `.pj-mini.av`)
- **기본값은 결정적.** `avatarDefault(name)`이 이름 해시로 캐릭터를 고르므로 "설정
  안 한" 에이전트도 항상 얼굴이 있고, 새로고침해도 바뀌지 않습니다. 선택은 옵션이지
  퍼널이 아닙니다
- 항목에 `src` 대신 `draw:()=>'<svg 내부>'`를 주면 인라인 SVG로도 그려집니다 — 예비 경로

**제약 두 가지** — ①Claude 클라우드 세션은 **이미지를 생성할 수도, 외부에서 내려받을
수도 없습니다**(프록시가 외부 HTTPS 차단). 파일은 Q가 채팅에 첨부(zip 가능 — `unzip`
있음)하거나 저장소에 직접 커밋하고, Claude는 배치·레지스트리 연결·화면 반영을 맡습니다.
②샌드박스에 PIL·ImageMagick이 없지만 **헤드리스 크로미움 스크린샷으로 리사이즈·정사각
크롭은 됩니다** — `<img style="width:512px;height:512px;object-fit:cover;object-position:50% 30%">`
하나짜리 HTML을 `--window-size=512,512 --screenshot`으로 찍으면 그 PNG가 결과물입니다
(a01.png 512→128px 테스트: 2.2KB). 손실 압축·포맷 변환은 못 하니 그건 Q 쪽에서.
③**채팅 메시지에 인라인으로 붙인 이미지는 디스크에 안 옵니다.** zip이나 파일 첨부로
올려야 `/root/.claude/uploads/<세션>/`에 떨어집니다. 이모지는 OS마다 생김새가
달라 프로필용으로는 쓰지 않습니다.

### 새 화면 추가 절차 (전부 해야 함)

1. `index.html`의 뷰 구역에 `<section class="view" id="view-이름">` 추가
2. 전용 스타일이 있으면 `css/`에 새 파일을 만들고 `<head>`의 링크 목록에 추가
3. 전용 로직이 있으면 `js/`에 새 파일을 만들고 하단 스크립트 목록의 **올바른 순서
   위치**에 추가
4. `js/03-data.js`의 `VIEWS` 배열 + `js/05-router.js`의 `sync()`/`go()` +
   `js/14-init.js`의 해시 처리 블록 (`AGENTS`에 임시 등록돼 있던 에이전트를 정식
   화면으로 만드는 경우에는 `AGENTS`에서 제거)
5. `js/08-notes.js`의 `NOTE_CTX`에 항목 추가
6. 사이드바(`index.html`의 `.sb`)에 진입점 추가
7. 버전 로그 + 드로어 버전 배지 갱신 → 검증

---

## 검증 루틴 — 수정 후 매번 실행

### ① 구문 체크

```bash
for f in js/*.js; do node --check "$f" || echo "FAIL $f"; done
```

### ② 렌더 확인 — 헤드리스 크로미움 1920×1080 스크린샷을 찍어 **눈으로 봅니다**

```bash
chrome --headless=new --disable-gpu --no-sandbox --window-size=1920,1080 \
  --hide-scrollbars --virtual-time-budget=3000 \
  --screenshot=/tmp/shot.png "file://$PWD/index.html#master"
```

Claude Code 클라우드 세션에는 크로미움이 미리 설치돼 있습니다:
`/opt/pw-browsers/chromium-*/chrome-linux/chrome` (버전 디렉터리는 `ls`로 확인).
폰트 로드가 프록시에 막혀 해시당 수십 초 걸릴 수 있으니, 여러 장 찍을 땐
백그라운드로 돌리세요.

새 로드이므로 해시가 바로 적용됩니다. Playwright 등으로 **로드 후 해시를 바꾸는
경우에는 반드시 `reload()`를 호출하세요** — 해시 라우팅은 초기화 시점에만 읽히므로,
빠뜨리면 이전 화면이 찍히고 "정상"으로 오판하게 됩니다.

최소 확인 해시:
`#drive` `#drive-list` `#master` `#project-p1` `#project-p2` `#solver` `#generator`
`#figure` `#canvas` `#chat` `#ppt` `#edit-doc` `#edit-xlsx` `#edit-ppt` `#edit-pdf` `#edit-yt`
`#guide` `#onboarding` `#onboarding-3` `#onboarding-5` `#gdrive`

JS가 끝까지 실행됐는지는 `--dump-dom`으로도 확인할 수 있습니다
(예: 드라이브 타일이 렌더됐는지 `grep -o 'class="tile"' | wc -l` → 11).

### ③ 런타임 에러

`pageerror` 리스너로 확인합니다. 콘솔의 `net::ERR_TUNNEL_CONNECTION_FAILED`는
샌드박스 네트워크 제한으로 폰트를 못 불러온 것이라 무시해도 됩니다(폴백으로 렌더됨).

### ④ 인터랙션 회귀

아래 지뢰 목록을 기준으로 확인합니다. 좌표 겹침 같은 건 `getBoundingClientRect()`로
스크립트 검사하는 편이 눈보다 확실합니다.

---

## 손대면 깨지기 쉬운 것 (실제로 겪은 지뢰)

1. **스크립트 태그 순서 = 전역 정의 순서.** js 20개는 하나의 전역 스코프입니다.
   `index.html` 하단의 로드 순서에서 정의(`const`)가 사용보다 뒤로 가면 통째로
   죽습니다. 새 JS 파일은 의존하는 파일 뒤에 넣으세요
2. **클래스명 충돌.** 최상위 레이아웃이 `.app { height:100vh }`를 씁니다. `.app` 같은
   흔한 이름을 새 배지·칩에 붙이면 그 전역 규칙에 걸려 작은 배지가 화면 전체 크기로
   부풀어 오릅니다. 새 클래스에는 반드시 접두사를 붙이세요
   (`.pj-`, `.ma-`, `.pjf-`, `.pmk-`)
3. **하단 중앙 「Q의 의견」 버튼과의 겹침.** v7.11부터 이 버튼이 화면 하단 중앙에
   고정돼 있습니다 (`left:50%` · `bottom:22px` · 높이 40px → 아래에서 62px까지 차지).
   화면 하단에 붙는 UI — 프로젝트 채널 컴포저 힌트, 채팅 입력바, 마스터 본문 — 는
   하단 여백 **74px**을 확보해 12px 간격을 둡니다. 버튼 크기·위치를 바꾸면 이 여백들도
   같이 조정하세요.
4. **동적 렌더 후 이벤트 재바인딩.** `renderProject()`는 `innerHTML`을 통째로 교체합니다.
   그 안의 버튼·탭·입력 리스너는 렌더 직후 다시 붙여야 합니다. 렌더를 다시 호출하는
   모든 경로(이름 변경, 탭 전환, 업로드 완료)에서 유지되는지 확인하세요
5. **전역 셀렉터의 스코프.** `$$('.sugg')` 같은 전역 수집은 다른 화면의 동명 요소까지
   잡습니다. `$$('#editor .sugg')`처럼 스코프를 좁히세요
6. **뷰 전환 상태.** 새 화면 추가 시 **`VIEWS` 배열(`js/03-data.js`) · `sync()`/`go()`
   (`js/05-router.js`) · init 해시 블록(`js/14-init.js`)** 을 같이 손봐야 합니다
7. **ESC 우선순위 체인** (`js/07-editor-open.js`). 현재 순서는 드라이브 피커 →
   Google Drive 모달 → 새 프로젝트 모달 → 파일 ⋯ 메뉴 → 드라이브 신규 메뉴 →
   첨부 메뉴 → Q의 의견 드로어 → 프로필 패널 → 편집기입니다. 새 모달·오버레이를
   만들면 이 체인에 끼워 넣으세요. **온보딩 오버레이는 의도적으로 이 체인에 없습니다**
   — 버튼(건너뛰기·완료)으로만 닫습니다
8. **업로드 목적지 라우팅** (`js/13-upload.js`). 업로드는 드라이브로 갈 수도, 특정
   프로젝트 폴더로 갈 수도 있습니다(`dest`). 목적지에 따라 토스트 문구·완료 후 이동
   버튼·개수 동기화 대상이 달라집니다. 업로드 관련 수정 시 두 경로를 모두 확인하세요

### CSS `color-scheme` 을 지우지 마세요

`css/00-base.css`의 `:root { color-scheme: light }` 는 **다크 모드 뷰어에서 폼
컨트롤이 어두워지는 것을 막는 장치**입니다. 삭제하면 안 됩니다.

---

## 작업 흐름

### 브랜치 — `main`에서 직접 작업합니다.

| 브랜치 | 용도 |
|---|---|
| `main` | 기본 브랜치 · **배포 브랜치 · 작업 브랜치**. 개발 · 커밋 · 푸시를 전부 여기서 합니다 |
| `claude/*` | **평소에는 쓰지 않습니다.** 대규모 리팩터링처럼 "배포 전에 먼저 보고 결정하고 싶은" 작업일 때만 Q가 지시해서 임시로 팝니다 |

### main 직접 작업의 안전장치 4개 — 브랜치가 하던 역할을 대신합니다

1. **푸시 = 즉시 배포. 검증 없이 푸시하지 않습니다.** 아래 「검증 루틴」 전체(구문 체크 →
   스크린샷 육안 확인 → 런타임 에러)를 푸시 **전에** 돌립니다. 브랜치가 없으므로 이게
   유일한 방어선입니다. 실제로 v7.9에서 스크린샷 검수로 CSS 버그 2건이 배포 전에 걸러졌습니다
2. **작업 시작 시 · 푸시 직전에 `git fetch origin`.** 컨테이너의 클론은 스냅샷에서 복원되어
   원격 참조가 낡아 있을 수 있습니다 — 실제로 `origin/main`이 5일 전 상태로 굳어 있어
   "배포가 깨졌다"는 오진을 낸 적이 있습니다. 파일 mtime이 최신이어도 클론이 최신이라는
   뜻이 아닙니다
3. **세션은 한 번에 하나만.** 두 세션이 동시에 `main`에 푸시하면 충돌합니다
4. **`main`에 force-push · rebase · amend 금지.** 되돌릴 때는 `git revert <커밋>`으로 새
   커밋을 쌓습니다(히스토리를 고쳐 쓰면 Q의 로컬 클론이 깨집니다). revert가 외과적으로
   되도록 **커밋은 작게** 유지하세요

### 세션 시작 체크리스트 — 새 Claude 세션은 이것부터

Q는 클라우드 세션(Claude Code 웹)에서 작업을 시킵니다. 새 세션은 컨테이너에 저장소를
새로 클론하고 대화 맥락 없이 시작하므로, 코드를 고치기 전에:

1. `git fetch origin` → `git status`로 **`main`이 최신인지 확인하고 `main`에서 작업**
   (fetch를 건너뛰지 마세요 — 안전장치 2번). 세션 지시문에 `claude/*` 브랜치가
   지정돼 있더라도 그건 하네스 기본값이므로, **이 저장소의 실제 작업 브랜치는
   `main`이라고 Q에게 확인**한 뒤 진행하세요
2. `index.html` 최상단 **버전 로그**로 현재 버전 파악, `git log --oneline`으로 직전 작업 확인
3. 이 문서를 읽었으면 **첫 응답에서 보고**: 현재 버전 · 직전 작업 한 줄 요약 ·
   검증 루틴 실행 준비 여부 — 그리고 무엇부터 할지 Q에게 물을 것
4. **코드를 미리 고치지 마세요.** Q의 요청을 받고 나서 움직입니다
