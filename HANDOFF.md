# GPAI 프로토타입 — 인수인계 지시서

`gpai-prototype.html` 원본과 함께 새 대화에 붙여넣는 문서.

**이 문서는 "작업하는 방법"만 다룬다.** 제품이 무엇이고 각 화면이 왜 그렇게 만들어졌는지는 전부 HTML 안에 내장돼 있으므로 여기서 반복하지 않는다. 픽셀·문구·데이터의 원본도 코드다(이 문서와 다르면 코드가 우선).

---

## 0. 시작하기 전에 반드시 할 일

1. **파일 최상단 주석의 버전 로그를 읽어라.** v1부터 현재까지 무엇이 언제 왜 추가됐는지 히스토리가 전부 있다.
2. **파일을 브라우저로 열고 우하단 「Q의 의견」 버튼을 눌러라.** 이 드로어 안에 제품 정의, 슬로건과 포지셔닝, 3단 에이전트 위계, 학교 비유, 프로젝트 AI의 메모리 축적 방식, 설계 원칙 6개, 아직 목업인 영역, 공유 규칙, 받고 싶은 피드백이 전부 들어 있다.
3. **각 화면으로 이동하며 드로어 최상단 "지금 화면" 섹션을 읽어라.** 14개 화면마다 그 화면의 설명 · **왜 이렇게 만들었나** · 해볼 것 · 작성자의 질문이 붙어 있다. 화면별 설계 의도의 정본이다.

즉 **제품 맥락의 정본은 HTML 안에 있다.** 이 문서를 읽고 나서 위 3단계를 먼저 수행하라.

---

## 1. 너의 역할

- 첨부된 `gpai-prototype.html`은 지금까지 만들어온 제품 프로토타입이다. 너는 **이 파일을 이어서 수정하는 프로토타이핑 파트너**다.
- **처음부터 다시 만들지 마라.** 기존 구조·스타일·네이밍 규칙을 따라 요청받은 부분만 고친다.
- 계속 대화하며 이 파일의 완성도를 높여 나간다. **한국어로 소통**한다.
- 작성자(Q)는 **코드를 직접 만지지 않는다.** 따라서 노트 문구 한 줄 수정도 전부 코드 수정으로 처리한다.
- 작성자는 컴퓨터공학 전공자다. 기술적 설명을 요구하면 추상적 비유 대신 실제 메커니즘(권한 모델, 동시성, 렌더링 파이프라인 등)으로 답해도 된다. 다만 **네가 아는 서비스/도구를 언급할 때는 그게 무엇인지부터 설명하고 내부 동작으로 들어가라.**

---

## 2. 절대 원칙

1. **단일 HTML 유지.** CSS/JS/SVG 전부 인라인, 외부 의존은 Google Fonts 링크 하나뿐.
   이유: 파일 하나를 슬랙에 던지면 누구든 더블클릭으로 열 수 있는 **"공유 마찰 제로"**가 이 프로토타입의 운영 방식이다.
2. **이미지 파일 금지.** 모든 그래픽 — 아이콘, 썸네일, 문서 목업, 다이어그램, 분자구조 — 은 인라인 SVG로 직접 그린다.
3. **기준 환경은 1920×1080 PC 크롬.**
4. **버전 로그를 남긴다.** 수정할 때마다 파일 최상단 주석에 한 줄 추가: `vX.Y (YYYY-MM-DD): 변경 요약`.
5. **새 화면을 추가하면 `NOTE_CTX`에 항목을 반드시 추가한다.** 필드는 `name / desc / why / try[] / ask`. 드로어에 설명이 안 뜨는 화면이 있으면 안 된다.
6. **하단 기밀 문구 바를 제거하거나 가리지 마라.** 모든 화면에 고정으로 노출돼야 한다.

---

## 3. 검증 루틴 — 수정 후 매번 실행

### ① 구문 체크

```bash
python3 -c "import re; h=open('gpai-prototype.html').read(); open('/tmp/c.js','w').write('\n'.join(re.findall(r'<script>(.*?)</script>',h,re.S)))"
node --check /tmp/c.js
```

### ② 렌더 확인 — 헤드리스 크로미움 1920×1080 스크린샷을 찍어 **눈으로 본다**

- **해시 라우팅은 초기화 시점에만 읽는다.** `page.goto(FILE+'#master')` 뒤에 반드시 `reload()`를 호출하라. 빠뜨리면 이전 화면이 찍히고 "정상"으로 오판하게 된다.
- 최소 확인 해시:
  `#drive` `#drive-list` `#master` `#project-p1` `#solver` `#generator` `#figure` `#canvas` `#chat` `#report-writer` `#edit-doc` `#edit-xlsx` `#edit-ppt` `#edit-pdf` `#edit-yt` `#guide`

### ③ 런타임 에러

`pageerror` 리스너로 확인한다. 콘솔의 `net::ERR_TUNNEL_CONNECTION_FAILED`는 샌드박스 네트워크 제한 때문에 폰트가 못 불려온 것이라 무시해도 된다(폰트는 폴백으로 렌더된다).

### ④ 인터랙션 회귀

아래 §5의 지뢰 목록을 기준으로 확인한다. 좌표 겹침 같은 건 `getBoundingClientRect()`로 스크립트 검사하는 편이 눈보다 확실하다.

---

## 4. 코드 수정 방식

2,900줄이 넘는 단일 파일이므로 대규모 편집은 **파이썬 치환 스크립트**로 처리하는 것이 안전하다. 핵심은 다음 한 가지다.

> **각 치환마다 매칭 횟수를 세고, 기대값과 다르면 파일을 쓰지 말고 즉시 중단시켜라.**

긴 파일에서 일부만 적용된 채로 넘어가는 것이 가장 나쁜 실패 모드다.

```python
R=[]                      # (old, new, expected_count)
def rep(o,n,c=1): R.append((o,n,c))

rep('<원본 문자열>', '<새 문자열>')
...

n=0
for o,new,c in R:
    got=h.count(o)
    if got!=c: print('MISMATCH(%d/%d): %r'%(got,c,o[:70])); n+=1
    else: h=h.replace(o,new)
if n: sys.exit(1)          # 하나라도 어긋나면 파일을 쓰지 않는다
```

문자열 앵커는 **공백과 줄바꿈까지 정확히** 일치해야 한다. 매칭에 실패하면 추측하지 말고 `grep -n` / `sed -n 'X,Yp'`로 실제 바이트를 먼저 확인하라.

---

## 5. 손대면 깨지기 쉬운 것 (실제로 겪은 지뢰)

1. **클래스명 충돌.** 최상위 레이아웃이 `.app { height:100vh }`를 쓴다. `.app` 같은 흔한 이름을 새 배지·칩에 붙이면 그 전역 규칙에 걸려 작은 배지가 화면 전체 크기로 부풀어 오른다. 새 클래스는 반드시 접두사를 붙여라 (`.pj-`, `.ma-`, `.pjf-`, `.confid-`).
2. **하단 고정 기밀 바(높이 약 35px)와의 겹침.** 화면 하단에 붙는 UI — 프로젝트 채널 컴포저 힌트, 채팅 입력바 — 는 하단 여백을 확보해야 한다. 바의 높이를 바꾸면 이 여백들도 같이 조정해야 한다.
3. **동적 렌더 후 이벤트 재바인딩.** `renderProject()`는 `innerHTML`을 통째로 교체한다. 그 안의 버튼·탭·입력 리스너는 렌더 직후 다시 붙여야 한다. 렌더를 다시 호출하는 모든 경로(이름 변경, 탭 전환, 업로드 완료)에서 이게 유지되는지 확인하라.
4. **전역 셀렉터의 스코프.** `$$('.sugg')` 같은 전역 수집은 나중에 만든 다른 화면의 동명 요소까지 잡아버린다. `$$('#editor .sugg')`처럼 스코프를 좁혀라.
5. **뷰 전환 상태.** `state` 객체에 `view / project / pjTab / editor / layout / q`가 들어 있다. 새 화면을 추가하면 **`VIEWS` 배열 · `sync()` · `go()` · init의 해시 처리 블록** 네 군데를 같이 손봐야 한다.
6. **ESC 우선순위 체인.** 현재 순서는 드라이브 피커 → 첨부 메뉴 → 새 프로젝트 모달 → Q의 의견 드로어 → 편집기다. 새 모달·오버레이를 만들면 이 체인에 끼워 넣어라.
7. **업로드 목적지 라우팅.** 업로드는 드라이브로 갈 수도, 특정 프로젝트 폴더로 갈 수도 있다(`dest`). 목적지에 따라 토스트 문구·완료 후 이동 버튼·개수 동기화 대상이 달라진다. 업로드 관련 수정 시 두 경로를 모두 확인하라.

---

## 6. 코드 지도 (탐색용 — 상세는 파일을 직접 읽어라)

- **`<style>`** — 토큰 → 사이드바 → 드라이브 → 챗 패널 → 업로드 토스트 → 3단 위계(마스터·프로젝트) → 파일 트리 → 기밀 바 → Q의 의견 드로어 → 솔버 → 제너레이터 → visuals → Chat → 편집기 → GPAI Chat → 첨부/피커 순. 섹션 주석이 달려 있다.
- **`<body>`** — `.app`(사이드바 + 뷰들 + 드라이브 챗) → `#editor` 오버레이(공통 프레임 + 5종 본문 + GPAI Chat) → 기밀 바 → 업로드 토스트 → Q의 의견 드로어 → 첨부 메뉴 / 드라이브 피커 / 새 프로젝트 모달.
- **`<script>` 주요 자료구조** — `driveItems`(드라이브 항목) · `PROJECTS`(프로젝트 + 파일 트리 + memory + pins) · `FOLDER_FILES`(신규 프로젝트 생성 시 폴더별 프리셋) · `CV_ITEMS`(캔버스 갤러리) · `NOTE_CTX`(화면별 메모) · `MODELS` · `state`.
- **`<script>` 주요 함수** — `go` / `sync`(라우팅) · `renderDrive` · `renderProjects` / `renderProject` / `pjTreeFlat` / `renderPjfList`(프로젝트·IDE형 파일 트리) · `pjSendMsg`(쓰레드 응답) · `openEditor` / `edSend` · `openAttMenu` / `openPicker` · `startDriveUpload` / `upFinish`(업로드 라우팅) · `renderNoteCtx`.

---

## 7. 파일과 배포

- **정본은 로컬 `New GPAI/gpai-prototype.html` 하나뿐이다.** 수정하면 같은 경로에 덮어쓴다.
- 과거에 Claude Artifact(claude.ai의 정적 페이지 호스팅)에 한 번 게시한 이력이 있으나 **현재는 게시 보류 상태**다. 사용자가 명시적으로 요청하기 전에는 게시하지 마라.
- 게시를 재개하는 경우, 마스터에서 **파생본을 빌드**해야 한다:
  - Artifact는 게시 시 `<!doctype>` / `<html>` / `<head>` / `<body>` 스켈레톤을 자동으로 씌운다. 따라서 게시본에서는 그 래퍼 태그를 제거하고 **`<title>` + 폰트 링크 + `<style>` + body 내용**만 남겨야 한다.
  - `<head>`의 `<meta name="color-scheme">`가 함께 사라지므로, 그 역할을 CSS `:root { color-scheme: light }`로 이미 옮겨 두었다. **지우지 마라.** 다크 모드 뷰어에서 폼 컨트롤이 어두워지는 것을 막는다.
  - 게시본은 빌드 산출물이다. 직접 수정하지 말고 항상 마스터에서 다시 생성하라.

---

## 8. 보안 · 공유 규칙

- 이 프로토타입은 **튜링 회사 내부용**이며 외부 유출·공유가 금지돼 있다.
- 하단 기밀 바 문구를 임의로 바꾸지 마라: **"튜링 회사 내부용이며, 외부로의 유출과 공유를 엄격히 금지합니다."**

---

## 9. 다음 이터레이션 후보

드로어의 「아직 목업이에요」 섹션에 최신 목록이 있다. 현재 가장 큰 건은 **문서 작성(Report Writer) 화면** — 유일하게 설계되지 않은 에이전트다.

---

## 10. 첫 응답으로 할 것

파일과 이 문서를 다 읽었으면 **코드를 미리 고치지 말고** 다음을 확인한 뒤 무엇부터 작업할지 물어라.

1. 버전 로그 기준 현재 버전
2. 「Q의 의견」 드로어에서 파악한 3단 위계 한 줄 요약
3. 검증 루틴(구문 체크 + 헤드리스 스크린샷) 실행 준비 완료 여부
